// declaring variable in c 

#include<stdio.h>
int main()
{
    int myvariable = 15; //declare variable and assign value
    printf("print the value store in the my variable : " "%d\n", myvariable); //must want to specify the format specfier 
    return 0;
}